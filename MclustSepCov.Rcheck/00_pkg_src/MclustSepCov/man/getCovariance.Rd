% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/RcppExports.R
\name{getCovariance}
\alias{getCovariance}
\title{Generate temporal covariance matrices}
\usage{
getCovariance(q, rho, type)
}
\arguments{
\item{q}{Dimension of a covariance matrix.}

\item{rho}{Temporal correlation in \eqn{(-1, 1)}. For \code{type='CS'}, \eqn{-1/\sqrt{q-1}} is a lower bound of \code{rho} for the returned matrix to be positive definite.}

\item{type}{A character string indicating one of types of temporal structure; autoregressive model if \code{type='AR'} and compound symmetry model if \code{type='CS'}. See `Details' for their structures.}
}
\value{
A \code{q}-by-\code{q} temporal covariance matrix with diagonals 1.
}
\description{
Return a covariance matrix with temporal structure.
}
\details{
Following temporal structures are available. 
\enumerate{
  \item Autogressive structure: \eqn{V = \big(\rho^{|i-j|}; 1\le i,j \le q\big)}.
  \item Compound symmetry structure: \eqn{V = \big({\rm I}(i=j) + \rho {\rm I}(i \neq j); 1\le i,j \le q\big)}.
}
}
\examples{
getCovariance(3, 0.3, "AR") # AR
getCovariance(3, 0.3, "CS") # CS

# AR structure with heterogeneous variances
hvar <- c(1, 2, 3) # variances
diag(sqrt(hvar)) \%*\% getCovariance(3, 0.3, "AR") \%*\% diag(sqrt(hvar))
}
